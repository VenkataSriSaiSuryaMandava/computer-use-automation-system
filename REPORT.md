```markdown
# Design Report: Computer-Use Automation System

## 1. Architecture
The architecture strictly decouples the exploratory, stochastic discovery phase from...
- **Discovery Phase (LLM-in-the-loop):** Operates against a live UI to explore pathwa...
- **Capability Compiler:** Translates the discovery session into a versioned, serial...
- **Deterministic Replay Engine (Zero-LLM):** Executes the compiled flow using a mult...

## 2. Artifact Schema
The `CapabilityArtifact` represents a formal capability contract decoupled from conve...
- **Typed Input/Output Contracts:** Defines parameters (e.g., `member_id`) and outpu...
- **Dynamic Variable Interpolation:** Uses Jinja-style parameter tokens (`{{inputs.m...
- **Multi-Strategy Locators:** Encapsulates accessibility roles, text content, CSS s...
- **Checkpoints:** Declares post-action assertions (`url_contains`, `element_visible...

## 3. Determinism & Error Handling
Replay determinism eliminates model latency, non-deterministic reasoning, and token c...
- **Locator Strategy:** Prioritizes Accessibility Tree nodes (`get_by_role`, `get_by_...
- **3-Tier Error Taxonomy:**
  1. *Expected Business Outcomes:* Catches known institution states (e.g., `404 Membe...
  2. *Recoverable Conditions:* Employs auto-wait polling and retries for network late...
  3. *Hard Failures:* When locator resolution is exhausted or assertions fail, the e...

## 4. Heterogeneity & Multi-Tenant
- **Surface Abstraction:** The execution engine communicates through driver primitive...
- **Multi-Tenant Inheritance:** Multiple credit unions often run identical vendor so...

## 5. Escalation & Handoff
When automation is blocked:
1. The engine pauses the active execution thread while maintaining the live Playwrig...
2. An intervention payload (failing step ID, diagnostic screenshot, error descriptio...
3. The operator manipulates the active browser window to resolve the issue (e.g., so...
4. Upon entering `resume`, the engine re-evaluates the page DOM state, logs the huma...

## 6. Safety
- **Host Allowlisting:** Network navigation is restricted to pre-approved institutio...
- **Irreversible Operation Gates:** Workflows flagged as `reversible: false` (such a...
- **PII / Secret Scrubbing:** Regex scrubbers sanitize SSNs, credit card numbers, an...

## 7. Cuts
- **Deliberately Cut:** A full WebRTC co-browsing operator portal UI (mocked via act...
- **Next to Build:** Automated shadow-run locator self-healing and tenant configurat...
```